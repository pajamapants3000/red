* allow whitespace inside address expression
* sub
* undo
* global (+int, rev)
* // and ?? repeat last search
* replace "line_number" with "address" as in "set_line_number"
* replace "insert_here" and "insert_line" with "append..."


